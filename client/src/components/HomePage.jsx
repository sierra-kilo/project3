import React from 'react';
import { Card, CardTitle } from 'material-ui/Card';


const HomePage = () => (
  <Card className="container">
    <CardTitle title="Settlement Check" subtitle="Your home for legal settlement searches." />
  </Card>
);

export default HomePage;
